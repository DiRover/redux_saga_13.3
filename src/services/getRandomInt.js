//нужно для получения рандомного значения индекса для массивка с картинками
//в json ссылки на картинки битые
export default function getRandomInt(max) {
    return Math.floor(Math.random() * max);
  }